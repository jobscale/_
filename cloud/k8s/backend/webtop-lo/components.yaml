apiVersion: apps/v1
kind: Deployment
metadata:
  name: webtop-lo
  labels:
    app: webtop-lo
    component: deployment
spec:
  selector:
    matchLabels:
      app: webtop-lo
  template:
    metadata:
      labels:
        app: webtop-lo
        component: workload
    spec:
      containers:
      - image: ghcr.io/jobscale/reverse-proxy
        name: webtop-lo
        env:
        - name: BACKEND
          value: http://dark.jsx.jp:2999
---
apiVersion: v1
kind: Service
metadata:
  name: webtop-lo
  labels:
    app: webtop-lo
    component: service
spec:
  selector:
    app: webtop-lo
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 3000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: webtop-lo
  labels:
    app: webtop-lo
    component: route
spec:
  parentRefs:
  - name: web-gateway
    namespace: gateway-system
  hostnames:
  - "webtop-lo.jsx.jp"
  - "webtop-lo.x.jsx.jp"
  rules:
  - backendRefs:
    - name: webtop-lo
      port: 80
---
apiVersion: gateway.nginx.org/v1alpha1
kind: AuthenticationFilter
metadata:
  name: webtop-lo
  labels:
    app: webtop-lo
    component: auth
spec:
  type: Basic
  basic:
    realm: "Authentication Required"
    secretRef:
      name: basic-auth
