#!/usr/bin/env bash

main() {
  attempt=100
  cd /tmp
  while $attempt
  do
    attempt=$((( $attempt - 1 )))
    EXIST="$(ps auxf | grep [c]onky | head -n -1 | awk '{print $2}')"
    [[ "$EXIST" != "" ]] && kill $EXIST
    LANG=en_US.UTF-8 conky
    sleep 2.2
  done
}

main &
