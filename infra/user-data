#!/usr/bin/env bash
set -eu

{
  curl -sL git.io/upto-bullseye | bash
  curl -sL git.io/ramen | bash
  curl -sL git.io/susi | bash
  curl -sL git.io/user-add | bash
  usermod -aG docker jobscale
  git clone https://github.com/jobscale/_.git
}
