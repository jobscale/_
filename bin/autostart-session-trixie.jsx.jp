#!/usr/bin/env bash
set -eu

adjust-window() {
  sleep 2
  ~/_/x-window/adjust-window $@
}

check-audio() {
  sleep 36
  $HOME/bin/wait-of-busy
  . ~/bin/autostart-session-check-audio
}

main() {
  (cd $HOME/_ && git pull)
  check-audio
}

main
