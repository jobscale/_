#!/bin/bash -eu

list="$(xinput --list | grep slave | grep pointer | awk -F= '{print $2}' | awk '{print $1}')"

for n in $list
do
  bash --noprofile --norc -c "exec >/dev/null 2>&1; setsid xinput --set-prop $n 'libinput Natural Scrolling Enabled' 1"
done
